// Suma de tres enters

#include <iostream>
using namespace std;

int main() {
    int a, b, c;          // declaració de variables
    cin >> a >> b >> c;   // lectura de les dades d'entrada
    int s = a + b + c;    // càlcul del resultat
    cout << s << endl;    // escriptura de la dada de sortida
}
